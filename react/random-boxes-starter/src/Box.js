import React from 'react';

const box = (props) => {
  const style = {
    background: "red",
    width: "100px",
    height: "100px"
  }
  return (
    <div style={ style }></div>
  )
}

export default box;
